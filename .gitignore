.DS_Store
*.zip
*.log
.env
.env.*

# Local prototype leftovers from before the exact zip export.
# (assets/ now holds tracked launch assets; only the leftover file is ignored.)
assets/cadence-halftone.png
css/
js/
